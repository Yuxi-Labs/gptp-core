import { describe, it, expect } from 'vitest';

// Placeholder for anthropic provider tests

describe('anthropic provider', () => {
  it('should have tests', () => {
    expect(true).toBe(true);
  });
});
